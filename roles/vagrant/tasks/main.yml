---
# Idempotent way to build a /etc/hosts file with Ansible using your Ansible
# hosts inventory for a source. Will include all hosts the playbook is run on.
# Inspired from http://xmeblog.blogspot.com/2013/06/ansible-dynamicaly-update-etchosts.html

- name: "Build hosts file"
  lineinfile: dest=/etc/hosts regexp='.*{{ item }}$' line="{{ hostvars[item].ansible_eth1.ipv4.address }} {{item}}" state=present
  when: hostvars[item].ansible_eth1.ipv4.address is defined
  with_items: groups['all']

- shell: ruby --version | grep {{ ruby_version }}
  register: have_desired_ruby
  ignore_errors: true
  changed_when: have_desired_ruby.rc != 0

- include: ruby.yml
  when: have_desired_ruby|failed